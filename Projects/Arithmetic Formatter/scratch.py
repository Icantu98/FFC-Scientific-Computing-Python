Data = ["4444 + 4444", "4444 + 333", "4444 + 43", "4444 + 9"]

x = []
y = []
z = []

for item in Data:
    parts = item.split()#'\\s+',item
    print(parts)
    x = 
    


length = 4+1 # length of bigger one + 1
line = list()
while length > 0:
    line.append('-')
    #print(line)
    length = length - 1
#print(*line, sep='')

old = '''
print('Add: X is bigger')
            diff = (len(xStr) - len(yStr))
            print('Diff lentth:',diff)
            iLine = 5
            if diff > 3:
                xShiftlen = len(xStr) + diff
                iLine = xShiftlen
            else:
                xShiftlen = 4
                yShiftlen = 2
            print('xShiftlenth',xShiftlen)
            print('yShiftlength',yShiftlen)
            #print('shiftlen',shiftlen)
            line = [] # Seperation line
            while iLine > 0:
                line.append('-')
                iLine = iLine - 1
            print(xStr.rjust(xShiftlen))
            print('+',yStr.rjust(yShiftlen))
            print(*line, sep='')
            print(zStr.rjust(yShiftlen))
            '''